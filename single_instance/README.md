Quick start for creating VirtualBox VM intended for Oracle Database Server Single Instance (SI) deployment:

1. Create shared folder for VM using `mkdir -p $HOME\Downloads\VagrantShare`
1. Create VirtualBox VM using `vagrant up` command.