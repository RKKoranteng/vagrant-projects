# Oracle SI Database on OEL9 

Quickstart
```
# Create a directory to keep all vagrant related files for the VM
$ mkdir backstage
$ cd backstage

# Initialize the Vagrant configuration file </br>
$ vagrant init ubuntu/trusty64

# Start the VM
$ vagrant up
```